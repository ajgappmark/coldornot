CC=gcc
CFLAGS=-I.
SRC0=main.c
SRC1=menu.c
SRC2=interface.c
MAIN=coldornot
INSTALL=install
INSTALL_DIRECTORY=$(INSTALL) -dm755
INSTALL_PROGRAM=$(INSTALL) -Dpm 0755
 
all: $(MAIN)
 
$(MAIN): $(SRC)
	$(CC) $(SRC0) $(SRC1) $(SRC2) -o $(MAIN)
 
clean:
	$(RM) *~ $(MAIN)
 
install:
	$(INSTALL_DIRECTORY) $(DESTDIR)/usr/bin
	$(INSTALL_PROGRAM) $(MAIN) $(DESTDIR)/usr/bin/
 
uninstall:
	rm -f $(DESTDIR)/usr/bin/$(MAIN)

